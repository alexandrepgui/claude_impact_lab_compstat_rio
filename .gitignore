# Configurações locais do Claude Code
.claude/

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Arquivos temporários do Office (lock)
~$*

# macOS
.DS_Store

# Pipeline runtime outputs (regerados a cada run)
pipeline_audit.jsonl
review_queue.json
score_ranking.json
relato_estruturado.jsonl

# Secrets — NUNCA committar API keys
.env
.env.local
.env.*.local
*.key
secrets.json
